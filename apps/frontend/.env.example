# ── URL del backend ────────────────────────────────────────────
# Desarrollo local (frontend fuera de Docker):
VITE_API_URL=http://localhost:3003

# Producción (Vercel — apuntar al backend desplegado):
# VITE_API_URL=https://<backend-produccion>
