export enum RolUsuario {
  ADMIN = 'admin',
  EMPLEADO = 'empleado',
  CLIENTE = 'cliente',
  PROVEEDOR = 'proveedor',
}

export enum EstadoUsuario {
  ACTIVO = 'activo',
  BLOQUEADO = 'bloqueado',
  INACTIVO = 'inactivo',
}
